# Google Analytics 4 measurement ID (starts with "G-"). Optional — defaults to
# the live property's ID in app/layout.tsx. Set this only to override it (e.g.
# to point a staging/preview deploy at a separate GA property).
NEXT_PUBLIC_GA_MEASUREMENT_ID=
